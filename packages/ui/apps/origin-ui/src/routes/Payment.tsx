import React from 'react';
import { LoginApp, LoginAppProps } from '@energyweb/origin-ui-user-view';

const PaymentRoute = (props: LoginAppProps) => {
  return <LoginApp {...props} />;
};

export default PaymentRoute;
