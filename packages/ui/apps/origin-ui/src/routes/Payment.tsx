import React from 'react';
import { PaymentApp, PaymentAppProps } from '@energyweb/origin-ui-user-view';

const PaymentRoute = (props: PaymentAppProps) => {
  return <PaymentApp {...props} />;
};

export default PaymentRoute;
